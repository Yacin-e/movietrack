apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: movietrack-vs
  namespace: movietrack
spec:
  hosts:
    - "*"
  gateways:
    - movietrack-gateway
  http:
    - match:
        - uri:
            prefix: /api/movies
      route:
        - destination:
            host: catalog
            port:
              number: 8080
    - match:
        - uri:
            prefix: /api/tracking
      route:
        - destination:
            host: tracking
            port:
              number: 8080
    - route:
        - destination:
            host: frontend
            port:
              number: 80
